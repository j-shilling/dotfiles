# User-Level OAF Agent Configuration

This directory follows the [Open Agent Format (OAF)](https://openagentformat.com/)
v0.8.0 specification. It provides user-level agents, skills, and MCP configurations
that compose with project-level `AGENTS.md` files.

Deployed via GNU Stow from the `agents/` package in the dotfiles repo.