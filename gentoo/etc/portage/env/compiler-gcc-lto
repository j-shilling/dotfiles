CC="gcc"
CXX="g++"
AR="ar"
NM="nm"
RANLIB="ranlib"

COMMON_FLAGS="-flto=16 -march=native -O2 -pipe"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
